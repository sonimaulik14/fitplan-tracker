{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma migrate deploy && prisma generate && next build",
  "crons": [
    { "path": "/api/cron/reminders", "schedule": "30 12 * * *" },
    { "path": "/api/cron/weekly", "schedule": "30 3 * * *" }
  ]
}
